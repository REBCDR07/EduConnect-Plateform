// Ce fichier est une coquille pour la configuration future d'une API réelle qui dera gérerpar le backend.
// Pour le frontend, la logique se retrouve dans les services mockés.

const api = {
  // Exemple de configuration future avec axios ou fetch
  // baseURL: 'https://api.educonnect.com/v1',
  // headers: { 'Content-Type': 'application/json' }
};

export default api;